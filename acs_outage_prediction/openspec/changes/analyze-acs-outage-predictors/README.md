# analyze-acs-outage-predictors

Status: `FEASIBILITY_PILOT_ONLY`.

The exact inventory bridge reaches 81 of 660 ACS devices, but the cohort and outcome prevalence drift materially. The bounded source-readiness audit found no usable ordinary nested or flat dynamic telemetry; only root `_lastBoot` advanced under special reboot-event semantics.

The optical null is limited to MySQL `acs_raw_dump`. Separate Snowflake device-level optical telemetry exists, and the live audit makes `PROD_DB.PUBLIC.HOURLY_DEVICE_PING_INFLUX` the primary point-in-time candidate; DBT/STG are derived or omit availability metadata, while FCT/AVG are NAS-day derivatives. Join and prehistory coverage are strong enough for a separate pilot, but roughly half of devices had constant weekly optical values and a point-in-time matched screen found only negligible pre-incident separation. The external-source status is therefore `FEASIBILITY_PILOT_ONLY`, with a required label-lineage check and untouched chronological confirmation.

An actual 15-feature logistic development run now exists at `outputs/optical_outage_model_2026-08-12_v1/`. On 148,309 chronological test rows, combined PR-AUC was 0.2147 versus 0.2143 for time plus non-optical features; the `+0.00042` device-bootstrap interval crossed zero. Optical plus time reached only 0.0624 versus 0.0607 for time alone and harmed equal-device AP. The decision is `CURRENT_DEVELOPMENT_POOLED_OPTICAL_LIFT_ONLY`. The strong non-optical result may reproduce the formal ping-based detector and is not independent outage-prediction evidence until label lineage is confirmed.

The final corrected current-window run is `outputs/acs_outage_feasibility_2026-08-11_v14/`. Boot age, 6-hour/24-hour reboot counts, and inform staleness passed the mapped-training data gate, and their combined model improved pooled test-row PR-AUC. That result did not generalize within devices, failed calibration, had only one partner group with adequate paired support, and produced zero FDR-significant individual parameters. The secondary duration check is closure-verified complete-case evidence only and is also null after adjustment. The decision is `CURRENT_WINDOW_EXPLORATORY_ROW_RANKING_SIGNAL_ONLY`: no useful individual ACS outage predictor was demonstrated. Any stronger claim requires the unchanged locked analysis on an untouched period after 2026-08-10.

The auxiliary `outputs/acs_window_analysis_2026-08-11_v2/` bundle is a database-source-free, post-hoc, case-only event-aligned diagnostic derived from v14. It tested six fixed transformations across five lead windows and found no signal that was stable across train, validation, test, and the singleton-onset device sensitivity. Equal-onset summaries are descriptive only because devices recur across onset clusters. Draft v1 is superseded by v2's corrected inference and sanitized, self-contained provenance. The diagnostic decision is `CURRENT_WINDOW_POST_HOC_EVENT_ALIGNED_DIAGNOSTIC_ONLY`; v14 and its decision remain canonical.
