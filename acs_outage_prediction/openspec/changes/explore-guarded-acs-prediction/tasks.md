## 1. Protocol

- [x] 1.1 Freeze the parent hashes, 30-minute guard, two fixed endpoints, unchanged feature manifest, primary logistic contrast, and fixed random-forest sensitivity before scoring outcomes.

## 2. Implementation

- [x] 2.1 Add one source-free command with label, split, prohibited-input, minimum held-out class-device, minimum test mixed-device, output-schema, and privacy assertions.
- [x] 2.2 Write aggregate `audit.json`, `model_results.csv`, `feature_signal.csv`, and `report.md` only, with the exploratory status in every artifact.

## 3. Validation

- [x] 3.1 Run the self-check and full analysis into a fresh output directory.
- [x] 3.2 Audit result interpretation, hashes, guard attrition, device-aware uncertainty, and `git diff --check`.
- [ ] 3.3 Create a separate prospective confirmation change/runner that registers a genuinely untouched post-2026-08-10 input hash and chronological boundaries, then applies the frozen statistical protocol before any confirmed predictor claim. The v14-pinned sensitivity command is not the future-data runner.
